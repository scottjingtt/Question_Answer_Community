import { GhpageCountPipe } from './ghpage-count.pipe';

describe('GhpageCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GhpageCountPipe();
    expect(pipe).toBeTruthy();
  });
});
